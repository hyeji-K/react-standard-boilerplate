# React Standard Architecture Example

이 프로젝트는 Vite를 사용하여 생성된 표준적인 React 프로젝트 구조 예시입니다.
실무에서 자주 사용되는 폴더 구조(Feature-based 혹은 Layer-based)를 기반으로 구성되었으며, 각 폴더의 역할과 사용법을 학습할 수 있습니다.

## 📂 폴더 구조 설명

```
src/
├── assets/      # 이미지, 폰트 등 정적 리소스
├── components/  # 재사용 가능한 공통 UI 컴포넌트 (Button, Input 등)
├── context/     # 전역 상태 관리 (Theme, Auth 등)
├── hooks/       # 커스텀 훅 (useFetch, useInput 등)
├── layouts/     # 페이지 레이아웃 (Header, Footer 포함 구조)
├── pages/       # 라우트와 매핑되는 페이지 컴포넌트
├── services/    # API 호출 및 외부 서비스 로직
├── styles/      # 전역 스타일 및 스타일 유틸리티
├── utils/       # 공통 유틸리티 함수 (날짜 포맷팅 등)
├── App.jsx      # 라우팅 및 전역 Provider 설정
└── main.jsx     # 앱 진입점 (Entry Point)
```

## 🚀 개발 워크플로우 (권장 순서)

보통 새로운 기능을 개발할 때 다음과 같은 흐름으로 진행합니다:

1.  **서비스(API) 정의**: `services/` 폴더에 필요한 API 호출 함수를 작성합니다.
2.  **커스텀 훅 작성(필요시)**: 데이터를 가져오거나 로직을 재사용해야 한다면 `hooks/`에 작성합니다.
3.  **UI 컴포넌트 작성**: `components/`에 재사용 가능한 작은 단위의 컴포넌트를 만듭니다.
4.  **페이지 구성**: `pages/`에서 위 요소들을 조합하여 화면을 만듭니다.
5.  **라우팅 연결**: `App.jsx`에 새 페이지의 라우트를 추가합니다.

## 🛠 실행 방법

```bash
# 의존성 설치
npm install

# 개발 서버 실행
npm run dev
```

## 📝 주요 학습 포인트

- **Custom Hooks (`hooks/`)**: `useFetch` 예제를 통해 로직을 분리하는 방법을 확인하세요.
- **Context API (`context/`)**: `ThemeContext`를 통해 전역 상태(다크모드)를 관리하는 방법을 확인하세요.
- **Layout Pattern (`layouts/`)**: `MainLayout`을 통해 반복되는 헤더/푸터 처리를 확인하세요.
- **Services (`services/`)**: API 호출 코드를 컴포넌트에서 분리하여 `apiClient`로 관리하는 패턴을 확인하세요.
